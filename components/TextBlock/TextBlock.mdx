import { Meta, Primary, Canvas, Controls } from "@storybook/addon-docs/blocks" 

import * as TextBlockStories from "./TextBlock.stories"

<Meta of={TextBlockStories} />

# Bloco de texto (Text Block)

Esse é um componente de Bloco de Texto usado para exibir conteúdo textual com título em uma página.

This is a Text Block component used to display text content with a title on a page.

### Primário (Primary)
<Canvas of={TextBlockStories.Primary} />

## Documentação Interativa (Interactive Documentation)

### Teste (Try it out):

<Primary />
<Controls />
